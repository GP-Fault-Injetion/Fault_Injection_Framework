# Fault_Injection_Framework
An automated fault injection harness designed to test the resilience of the AUTOSAR Memory Stack. Utilizing a non-intrusive macro-hooking mechanism, it intercepts memory operations to inject configurable, time-based errors (Multi-bit flips, Burst errors, Stuck-at faults) via JSON test cases.
